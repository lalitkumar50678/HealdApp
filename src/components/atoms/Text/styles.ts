import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    primaryText:{
        fontSize: 16,
        color: 'black'
    },
    secondaryText: {
         fontSize: 12,
        color: 'black'
    }
});

export default styles;